# Dev/test dependencies (not needed at runtime)
-r requirements.txt
pytest>=8.0.0
httpx>=0.27.0   # FastAPI TestClient
# Pinned, not floored. `ruff>=0.6.0` let CI resolve to whatever was newest that
# day, so a wider default rule set turned CI red without a code change. Bump
# this deliberately and fix what the new version finds in that same commit.
ruff==0.16.3
